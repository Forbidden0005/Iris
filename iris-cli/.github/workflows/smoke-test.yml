name: CLI Smoke Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Type Check
        run: npx tsc --noEmit

      - name: Build CLI
        run: npm run build

      - name: Run Unit Tests
        run: npm test

      - name: Verify CLI Binary (Help)
        run: ./bin/iris.js --help

      # NOTE: `iris doctor` checks gateway reachability on localhost:5010.
      # In GitHub Actions this service is usually not running, so this step
      # can fail even when CLI code/build/tests are healthy.
      - name: Smoke Test - Doctor
        run: ./bin/iris.js doctor

      - name: Smoke Test - Auth
        run: ./bin/iris.js auth

      - name: Smoke Test - Sandbox Branching
        run: |
          ./bin/iris.js branch ci-test
          ./bin/iris.js branches | grep ci-test
          ./bin/iris.js test-sandbox --file ci-demo.txt --content "CI passed"
          ./bin/iris.js preview ci-test | grep "CI passed"
          ./bin/iris.js rollback ci-test

      - name: Verify Distribution Artifact
        run: |
          if [ ! -f dist/iris.mjs ]; then
            echo "Build artifact missing!"
            exit 1
          fi
