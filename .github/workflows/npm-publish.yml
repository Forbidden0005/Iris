name: Publish to npm

on:
  push:
    tags:
      - 'v*'           # iris: v0.9.0, v1.0.0, etc.
      - 'cli-v*'       # iris-cli:  cli-v0.2.0, etc.

jobs:
  publish-iris:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
      - run: npm install --ignore-scripts
      - name: Build dashboard
        working-directory: apps/dashboard
        run: |
          npm install --ignore-scripts
          npm run build
      - name: Build vibe
        working-directory: apps/vibe
        run: |
          npm install --ignore-scripts
          npm run build
      - name: Determine npm tag
        id: tag
        run: |
          VERSION="${GITHUB_REF#refs/tags/v}"
          if echo "$VERSION" | grep -qE '(alpha|beta|rc)'; then
            echo "tag=beta" >> "$GITHUB_OUTPUT"
          else
            echo "tag=latest" >> "$GITHUB_OUTPUT"
          fi
      - run: npm publish --access public --tag ${{ steps.tag.outputs.tag }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-iris-cli:
    if: startsWith(github.ref, 'refs/tags/cli-v')
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
      - name: Build iris-cli
        working-directory: iris-cli
        run: |
          npm install --ignore-scripts
          npm run build
      - name: Determine npm tag
        id: tag
        run: |
          VERSION="${GITHUB_REF#refs/tags/cli-v}"
          if echo "$VERSION" | grep -qE '(alpha|beta|rc)'; then
            echo "tag=beta" >> "$GITHUB_OUTPUT"
          else
            echo "tag=latest" >> "$GITHUB_OUTPUT"
          fi
      - name: Publish iris-cli
        working-directory: iris-cli
        run: npm publish --access public --tag ${{ steps.tag.outputs.tag }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
